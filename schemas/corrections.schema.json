{
  "$defs": {
    "CorrectionEntryModel": {
      "properties": {
        "correction_id": {
          "description": "ID chỉnh sửa dạng corr_XXX",
          "title": "Correction Id",
          "type": "string"
        },
        "correction_type": {
          "description": "Phân loại chỉnh sửa (ví dụ: price_update, unit_update)",
          "title": "Correction Type",
          "type": "string"
        },
        "field_path": {
          "description": "Đường dẫn trường dữ liệu bị chỉnh sửa, ví dụ: items[0].unit_price",
          "title": "Field Path",
          "type": "string"
        },
        "new_value": {
          "description": "Giá trị mới sau khi sửa",
          "title": "New Value"
        },
        "old_value": {
          "description": "Giá trị cũ trước khi sửa",
          "title": "Old Value"
        },
        "reason": {
          "description": "Lý do chỉnh sửa",
          "title": "Reason",
          "type": "string"
        },
        "timestamp": {
          "format": "date-time",
          "title": "Timestamp",
          "type": "string"
        },
        "user": {
          "default": "human",
          "description": "Tên người hoặc hệ thống thực hiện chỉnh sửa",
          "title": "User",
          "type": "string"
        }
      },
      "required": [
        "correction_id",
        "field_path",
        "old_value",
        "new_value",
        "reason",
        "correction_type"
      ],
      "title": "CorrectionEntryModel",
      "type": "object"
    }
  },
  "properties": {
    "corrections": {
      "description": "Danh sách các chỉnh sửa",
      "items": {
        "$ref": "#/$defs/CorrectionEntryModel"
      },
      "title": "Corrections",
      "type": "array"
    },
    "quotation_id": {
      "description": "ID báo giá gốc",
      "title": "Quotation Id",
      "type": "string"
    },
    "schema_version": {
      "default": "1.0",
      "description": "Phiên bản schema corrections",
      "title": "Schema Version",
      "type": "string"
    }
  },
  "required": [
    "quotation_id"
  ],
  "title": "CorrectionsFileModel",
  "type": "object"
}
